Це проста утиліта на Python для базової інспекції Python-коду. Вона підраховує:
- Кількість функцій
- Кількість класів
- Кількість умовних операторів (`if`)
- Кількість циклів (`for`, `while`)
- Кількість присвоєнь
